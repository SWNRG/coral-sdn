A Java Socket Server.
it runs automatically with threads
it will wait on localhost:8999
if it gets something from socks, it will print it back
it also waits for the user to enter something in the terminal. It will send this to the socks client(s)
